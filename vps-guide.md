# 📖 راهنمای اجرای همیشه روشن ربات قیمت خودرو روی سرور مجازی (VPS)

برای اینکه ربات شما پس از بستن ترمینال همچنان در پس‌زمینه سرور در حال کار باشد، دو روش استاندارد و بسیار قدرتمند زیر وجود دارد.

---

## 🚀 روش اول: استفاده از ابزار قدرتمند PM2 (پیشنهادی)

ابزار **PM2** محبوب‌ترین و منعطف‌ترین ابزار مدیریت پروسه برای برنامه‌های Node.js است. این ابزار به صورت خودکار خطاها را لاگ می‌کند، در صورت کرش کردن برنامه را ریستارت می‌کند و پس از ریبوت سرور مجازی نیز مجدداً ربات را بالا می‌آورد.

### مراحل نصب و استفاده:

۱. **ورود به پوشه ربات در سرور:**
   ```bash
   cd /path/to/your/iran-car-bot-builder
   ```

۲. **اجرای اسکریپت نصب و پیکربندی خودکار:**
   یک اسکریپت آماده با نام `setup-vps.sh` در ریشه پروژه قرار داده‌ایم که تمامی نیازهای شما (نصب پکیج‌ها، بیلد پروژه، نصب PM2 و مانیتورینگ ربات) را به صورت خودکار انجام می‌دهد. برای اجرای آن کدهای زیر را بزنید:
   ```bash
   chmod +x setup-vps.sh
   ./setup-vps.sh
   ```

۳. **دستورات پرکاربرد PM2 برای مدیریت ربات:**
   - **مشاهده وضعیت ربات:**
     ```bash
     pm2 status
     ```
   - **مشاهده لاگ‌های زنده ربات (برای بررسی قیمت‌یابی یا تعاملات کاربران):**
     ```bash
     pm2 logs iran-car-bot
     ```
   - **ریستارت کردن ربات:**
     ```bash
     pm2 restart iran-car-bot
     ```
   - **متوقف ساختن ربات:**
     ```bash
     pm2 stop iran-car-bot
     ```

---

## 🛠️ روش دوم: استفاده از سرویس سیستمی لینوکس (Systemd)

اگر تمایلی به استفاده از پکیج‌های اضافی ندارید، می‌توانید از سیستم‌سازگار بومی لینوکس (`Systemd Service`) استفاده کنید.

### مراحل راه‌اندازی سرویس لینوکس:

۱. **ایجاد فایل سرویس با دستور ویرایشگر (مانند nano):**
   ```bash
   sudo nano /etc/systemd/system/carprice-bot.service
   ```

۲. **کپی کردن کدهای زیر درون فایلی که باز شده است:**
   *(توجه: حتما آدرس فولدری ربات خود را به جای `/path/to/your/...` قرار دهید)*
   ```ini
   [Unit]
   Description=Iran Car Price Telegram and Bale Bot
   After=network.target

   [Service]
   Type=simple
   User=root
   WorkingDirectory=/path/to/your/iran-car-bot-builder
   ExecStart=/usr/bin/npm run start
   Restart=on-failure
   Environment=NODE_ENV=production

   [Install]
   WantedBy=multi-user.target
   ```

۳. **فعالسازی و راه‌اندازی سرویس:**
   ```bash
   # ریلود کردن تنظیمات سرویس‌های سیستم‌عامل
   sudo systemctl daemon-reload

   # فعال‌سازی اولیه سرویس برای لود شدن خودکار بعد از روشن شدن سرور
   sudo systemctl enable carprice-bot

   # استارت و روشن کردن ربات
   sudo systemctl start carprice-bot
   ```

۴. **دستورات کاربردی سرویس:**
   - **بررسی روشن بودن ربات:**
     ```bash
     sudo systemctl status carprice-bot
     ```
   - **دیدن لاگ‌ها و خطاهای فنی:**
     ```bash
     journalctl -u carprice-bot.service -f -n 100
     ```

---

## 🔒 پیکربندی پروکسی تلگرام (Telegram Proxy)

از آنجایی که سرورهای تلگرام در ایران فیلتر هستند، در صورتی که سرور مجازی شما داخل کشور (ایران) قرار دارد، برای فعالسازی ربات تلگرام خود نیاز به اعمال پروکسی خواهید داشت:

۱. وارد پنل مدیریت تحت وب ربات (پنل ادمین) کاربری خود شوید.
۲. در بخش **پیکربندی ربات** فیلد **پروکسی تلگرام (HTTP / SOCKS5)** را مشاهده خواهید کرد.
۳. مقدار پروکسی را بر اساس ساختار زیر وارد نموده و ذخیره کنید:
   - **پروکسی SOCKS5 (ساکس):**
     `socks5://127.0.0.1:1080` یا با یوزرنیم پسورد: `socks5://user:pass@IP:PORT`
   - **پروکسی HTTP:**
     `http://127.0.0.1:8080` یا با یوزرنیم پسورد: `http://user:pass@IP:PORT`
۴. پس از ذخیره، ربات مجددا خودکار با اعمال پروکسی راه‌اندازی می‌شود و ارتباط آن با تلگرام برقرار می‌گردد.
